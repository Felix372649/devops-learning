
#========================[ 字 符 串 ]=======================
# 字符串是字符的容器，一个字符串中可以存放任意数量的字符。如："Python" 'Python' """Python"""


#===========[ 这 tm 是 列 表 list ]============
# 正索引  0   1   2   3   4   5
# s = [ "p","y","t","h","o","n" ]
# 负索引 -6  -5  -4  -3  -2  -1
#=========================================
# 语法: [开始索引:结束索引:步长]
# 不包含结束索引位置对应的元素（开始索引未指定默认为0;结束索引未指定默认为-1;步长未指定默认为1）
# 索引采用正向、反向索引都可以
# 步长是选取间隔，默认步长为1（如果是-1，表示从后向前）

# 举例：
# s[0:5:1]切片后的
# s[0:5:2]切片后的
# print(s[0:5:1])  # 结果为"Pytho" 顾头不顾尾第5个索引-1不显示
# print(s[0:5:2])  # 结果为"Pto" 中间忽略一个索引!1、3、5被忽略了！


#==========[ 字 符 串 str ]==========
# s = "hello-python" # 这是字符串（str）不能进行修改操作的!
# print(s[4])
# print(s[-2])
# # s[4] = "x"         # 就像这个!修改了会报错! 除非用for i in s:
# print(s)

# 切片
# print(s[6::1])       # 6::1 中间不写数字,则代表显示到末尾python都显示出来了不会少个n


# 步长为正数:则从前往后截取! # 步长为负数:从后往前截取!
# print(s[-1:-5:-1])

# print(s[6:-1:-1])

#==========[ 字 符 串 str 常 用 方 法 :]==========
#  方法	              作        用	                                    样例
# find()	   在字符串中查找子串,返回第一次出现的索引位置,找不到返回-1	s.find('Python')
# count() 	   统计子串在字符串中出现的次数	                        s.count('H')
# upper()	   将字符串中的所有字母转换为大写	                        s.upper()
# lower()	   将字符串中的所有字母转换为小写	                        s.lower()
# split()	   将字符串按指定分隔符分割成列表	                        s.split(' ')
# strip()	   去除字符串两端的空白字符或指定字符	                    s.strip() / s.strip('*')
# replace()	   将字符串中的指定子串替换为新的子串	                    s.replace('H','C')
# startswith() 检查字符串是否以指定子串开头，返回布尔值	                s.startswith('P')

# s = "Hello-Python-Hello-World"
# # print(s.find("hello"))
# print(f"统计H出现次数:{s.count("H")}")
# print()
# # print(s.strip('-'))
# print(s.startswith("d"))
# print(s.replace("Hello","Good"))

# =========================[ 字 符 串 str 案 例 ]============================
# 1．邮箱格式验证：用户输入一个邮箱，验证邮箱格式是否正确（包含一个@和至少一个.），
# 如果输入正确，输出邮箱格式正确，否则输出邮箱格式错误。
# email=input("请输入邮箱:")
# if email.count("@") == 1 and email.count(".") >= 1:
#     print("邮箱格式正确")
# else:
#     print("邮箱格式错误")

#------[方式2]------
# in 运算符 判定字符串里是否存在， 存在True ，不存在False
# "." in email 就是判断点在不是email里
# if email.count("@")== 1 and "." in email:
#     print("邮箱格式正确")
# else:
#     print("邮箱格式错误")
# 总结
# 1．如何判断指定的子字符串是否出现在字符串之中?
# in 运算符,返回bool值

# ===========[ 作 业 ]============
# 1．输入一个字符串，判断该字符串是否是回文（两边对称）。
# 黄山落叶松叶落山黄
# 上海自来水来自海上
# good= "黄山落叶松叶落山黄"
# Luck="上海自来水来自海上"
# aa=input("请输入：")
# if aa[::-1] == aa:          #::-1就是反向取元素从右到左！默认是左右！
#     print("是回文")

# 2．将用户输入的10个字符串，反转后全部转换为大写，然后记录在列表中，最后将列表内容，遍历输出出来。
# bb=[]
# for a in range(10):               # 说人话就是：用户输入10次，每次输入的内容进行反转 并 转换成大写字符，然后写进列表里。最后讲列表所有内容一个一个打印出来！
#     aa = input("请输入:")
#     bb.append(aa[-1::-1].upper()) # ::-1就是反向取元素从右到左！默认是左右！
                                    # bb.append把内容写入列表最尾部（括号里负责把内容反转 并转换成大写）
# for i in bb:
#     print(i)

