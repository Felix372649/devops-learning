#==============[ 元 组 tuple ]================
# 1.列表有什么特点？
#   元素可重复、有序、可以修改的

# 2.如果要要记录一些信息，而这些信息，不能被修改，只能查询呢？
#   列表就不适合了
#   此时就可以考虑使用元组，元组与列表最大的不同点在于：元组一旦定义完成，不可修改


#-----------------【 基  本  操  作 】--------------------
# aa=(80,90,70,60,50,80)
# print(type(aa))       # 类型为tuple元组

# cc=(100)              # 这样定义类型就是int不是tuple
# dd=(100,"A",)             # 加个，豆号，就是元组了！包括 "A" !
# print(type(cc))       # 类型：int
# print(type(dd))       # 类型：tuple

# print(aa.index(90))   # 这样你就能查到90在tuple元组index索引里第一个90元素的位置!
# print(aa.count(80))   # 统计数量 有几个80的元素！


#==============[ 元 组 tuple 组包 与 解包]================
# 组包（Packing） ：将多个值合并到一个容器（元组、列表）中。
# 解包(Unpacking）：将容器(元组、列表)解开成独立的元素，分别赋值给多个变量。

# 定义元组，组包：
# t1 = (5,7,9,1)
# t2 =  5,7,9,1

# 基础解包：
# a,b,c,d = t1

# (*)扩展解包
# x, *y, z = t2  # x为5  y为[7,9]   z为1
                 # *y就是列表 将多个元素写入y里的意思
                 # x 和 z 为变量只能分一个元素！
                 # 严格按顺序从左 到右 分配元素！


# s, *o = t2     # s变量为5  o为列表[7,9,1]
                 # 元素充足的情况下!从左到右一个一个分配,没有优先权!

# *o, e = t2     # o为[5,7,9,]      e是最后分配为1   必须留最后一个元素给e
                 # 元素充足的情况下!从左到右一个一个分配,没有优先权!


#假设 t3 = (5, 1)，执行 x, *y, z = t3：
#  x  拿走左边的 5。
#  z  拿走右边的 1。
# *y  没东西可拿了，但它依然会遵守规则生成一个列表，结果就是：y 为空列表 []。


# t6=(1,2,3,4,5,6,)
# a,b,*c,d,e = t6
# 元素充足的情况下!从左到右一个一个分配,没有优先权!
# a1  b2  c34  d5  e6

# 元素有限的情况下！变量优先分配！ *带星的列表最后
# 只有带*的是列表！其他都为变量！


#--------[ 注 意 下！]---------
# t3=(1,2)
# a,b,c,d=t3
# 这种情况代码会直接报错，程序会直接停止运行。


# ----------------------[ 实 操 ]--------------------
# t1=(1,2)
# a,b,*Luck=t1         # # 元素有限的情况下！变量优先分配！a1  b2   *带星的列表没有分到 为空列表！
# print(a,b,Luck)
#
# t2=2,4,6,8,10
# *c,d,e=t2
# print(c,d,e)         # 元素充足的情况下!从左到右一个一个分配,没有优先权! c246 d8 e10



# ----------------------[ 案 例 ]--------------------
# 案例1：现有两个变量 a = 10      b = 20
# 现需要将这两个变量值交换，然后输出到控制台
# a=10
# b=20
# a,b = b,a
# print(a,b)

# 案例2：现有三个变量，分别为：a=100，b=200，c=300，
# 现需要将这三个变量值进行交换，将a,b,c的值分别赋值给c,a,b，并将其输出到控制台。

# a=100
# b=200
# c=300
# c,a,b = a,b,c
# print(a,b,c)

# ====================[ 总 结 ]==================
# 组包（Packing)：将多个值合并到一个容器（元组、列表）中。
# 解包(Unpacking）：将容器中的元素分别赋值给多个变量。



# ----------------------[元 组 tuple 终 极 案 例 ]--------------------

# 基于已学习的数据容器完成如下需求
# 根据提供的学生成绩单，完成如下需求：

# 1．计算每个学生的总分、各科平均分，然后一并输出出来。
# 2．统计各科成绩的最低分、最高分、平均分，并输出。
# 3．查找成绩优秀（平均分大于90）的学生，并输出.

#学号     姓名   语文 数学  英语
chengji=(
("S001","王林",  85,  92,  78,),
("S002","李慕婉",92,  88,  95,),
("S003","十三",  78,  85,  82,),
("S004","曾牛",  88,  79,  91,),
("S005","周轶",  95,  96,  89,),
("S006","王卓",  76,  82,  77,),
("S007","红蝶",  89,  91,  94,),
("S008","徐立国",75,  69,  82,),
("S009","许木",  86,  89,  98,),
("S010","遁天",  66,  59,  72,)
)


# 1．计算每个学生的总分、各科平均分，然后一并输出出来
print("姓名\t总分\t各科平均分")
#------[方式一]------
# for i in chengji:
#     print(f"{i[1]}\t{(i[2] + i[3] + i[4])}\t\t{((i[2] + i[3] + i[4]) / 3):.1f}")

#------[方式二 元组解包 - 推 荐 - ]------
for id, name, chinese, math, english, in chengji:
    print(f"{name}\t{chinese + math + english}\t\t{(chinese + math + english)/3:.1f}")


# 2．统计各科成绩的最低分、最高分、平均分，并输出。
chinese=  [s[2] for s in chengji] # for s in chengji循环每轮将元组里的一个索引中的元素写入s里
math=     [b[3] for b in chengji] # s[2] 就是s元组里2号索引的元素
english=  [y[4] for y in chengji] # 每轮循环将s的2号索引里的元素写入chinese列表变量里,等于chonese.append(s[2])

print(f"""
2.各科成绩(低/高/平均):
\t\t最低分\t最高分\t平均分
[语文]\t{min(chinese)}\t\t{max(chinese)}\t\t{sum(chinese)/len(chinese):.1f}
[数学]\t{min(math)}\t\t{max(math)}\t\t{sum(math)/len(math):.1f} 
[英语]\t{min(english)}\t\t{max(english)}\t\t{sum(english)/len(english):.1f}
""")


#3．查找成绩优秀（平均分大于90）的学生，并输出
print("姓名\t平均成绩")

# 看下面这里，元组解包 数量一定要一致！我少写个name一直报错！思考了很久都没解决！
# 元组有五个索引！你就得用五个变量去解包！
#   1     2      3       4      5        元组（五个索引）
for id, name, chinese, math, english, in chengji:   # id和name可以用_代替！
# for _, _, chinese, math, english, in chengji:     # 符号 _ 全行业约定俗成把它当做“垃圾桶”或者“占位符”来用。
    if ((chinese + math + english) / 3) > 90:
        print(f"{name}\t{(chinese + math + english)/3:.1f}")
